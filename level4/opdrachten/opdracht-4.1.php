<?php declare(strict_types=1);

function csvVelden(string $line): array
{
    // TODO:
    // Split op komma's en trim elk veld.
    // Voorbeeld: "a, b,  c" => ["a","b","c"]
    // Let op: het is expres "simpel CSV" (geen quotes/escaped commas).
    return [];
}

